name: Deploy to GitHub Pages

# mainへのpushをgh-pagesブランチへ反映する。
# GitHub Pagesはgh-pagesブランチを配信元として自動でサイトを更新する。
on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Push main to gh-pages
        run: git push origin HEAD:gh-pages --force
